Print environment variables.

No input prints all variables (KEY=VALUE format).
Provide variable names to print specific values.

Examples:
    (empty)    → prints all environment variables
    PATH       → prints the value of PATH
    HOME USER  → prints HOME and USER values
