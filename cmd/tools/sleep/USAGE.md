Pause for a specified duration.

A bare number is treated as seconds.
Go duration strings are also accepted.

Examples:
    5         → sleep 5 seconds
    0.5       → sleep 500ms
    300ms     → sleep 300 milliseconds
    2h45m     → sleep 2 hours 45 minutes
