Print the directory component of one or more paths.

    PATH [PATH...]

Each path is printed on its own line.

Examples:
    /path/to/file.txt           → /path/to
    /usr/bin/ls /etc/hosts      → /usr/bin\n/etc
    file.txt                    → .
