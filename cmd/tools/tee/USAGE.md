Write input to file(s) and return it.

First line: [-a] FILENAME [FILENAME...]
Remaining lines: content to write.

Options:
    -a    append to files instead of overwriting

Example:
    output.txt
    Hello, world!
